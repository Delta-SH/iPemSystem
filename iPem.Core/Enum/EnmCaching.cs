namespace iPem.Core.Enum {
    /// <summary>
    /// Represents a cache provider enumeration
    /// </summary>
    public enum EnmCacheProvider {
        Memcached,
        Redis,
        MemoryCache,
        WebCache
    }
}
